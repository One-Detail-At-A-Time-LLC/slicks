import React from 'react'

/**
 * A React functional component that renders a non-member page.
 *
 * @return {JSX.Element} The JSX element representing the non-member page.
 */
const nonMemberPage = () => {
    return (
        <div>
            nonMemberPage
        </div>
    )
}

export default nonMemberPage
